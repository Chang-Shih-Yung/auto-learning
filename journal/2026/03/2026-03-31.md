---
title: "2026-03-31 AI 技術日誌"
date: "2026-03-31"
---

# 2026-03-31 AI 技術日誌

## Claude Code v2.1.88：Compound Command Hook 精準觸發 + Apple Silicon Voice Mode 全修

你現在可以在 M 系列 Mac 上用 `--voice` 直接開口說話控制 Claude Code，麥克風權限問題已在 v2.1.88（2026-03-30 發布）全面修復。同時，compound command（例如 `git add && git commit`）的 hook 條件過濾修好了誤判問題，排程工作流裡「指定指令才觸發」這件事現在行為可預期。記憶體洩漏（大型 JSON 輸入）與超過 1 GB 檔案的崩潰問題同步修復，長時間 session 更穩定。

Claude Code 的 alt-screen 渲染引擎換成 flicker-free 模式，大型輸出不再閃爍，terminal 捲動記憶不再消失。這不是功能更新，是一次把「用起來順」這件事做紮實的修復版本——StructuredOutput schema caching 的 bug 也一起修掉，讓依賴結構化輸出的工作流輸出更穩定。

以前你在 M 系列 Mac 上開語音模式常碰到麥克風授權失敗、或者 compound hook 觸發了不該觸發的動作；現在 v2.1.88 在 Apple Silicon 上的行為終於和官方文件描述的一致。如果你有用 hook 做自動化工作流，這次升級值得立刻更新。

<AnnotationCard
  relevance={5}
  skills={["ai-workflow", "react"]}
  adjacent="Claude Code Hooks DSL"
  adjacentNote={"你已會設定 Claude Code hooks——下一步：研究 compound command 條件語法，試著寫一個「on: git add && git commit → 自動執行 lint + 確認 journal 格式」的 hook chain，把現有排程任務的人工驗證步驟自動化。"}
  connection={"Claude Code 是你的主力 AI 工具，v2.1.88 的 compound hook 精準觸發修復直接影響 Claude_auto 排程任務的觸發邏輯——之前你在 session-log 裡手動確認的 idempotency check，未來有機會交給 hook 自動攔截。"}
/>

---

## Cursor 2.5：8 個並行 Agent 在隔離 VM 跑，BugBot Autofix 35% 直接 merge 進主分支

在 Cursor 2.5 裡，你可以同時啟動最多 8 個 AI agent，每個都跑在獨立的 Ubuntu VM 上，帶自己的 Git worktree——修改互不干擾、context 互不污染，任務結束後環境自動回收。BugBot 從「只出報告」升級為「找到問題、自己開 VM、測試修復、提 PR」，超過 35% 的 BugBot Autofix 被直接 merge 進主分支，不是玩具，是進入生產的自動修復流程。

這背後是 Cloud Agents 架構：每個 agent 拿到環境副本、有獨立的 CLI 存取和 Git worktree，coordinator 只負責分派 task，不共享中間結果。這是 2026 年主流 AI coding tool 解決 context pollution 的標準做法。同時推出的 MCP Apps（v2.6）讓 agent 在對話框裡直接渲染互動式圖表和白板——把「看 AI 輸出」從純文字升級成可操作的視覺介面。

以前你要同時跑多件事，只能開多個 terminal session 手動 stash 切換；現在 Cursor 的 8-VM pool 把這個協調層做掉了。BugBot 的 35% merge rate 意味著超過三分之一的 PR 問題可以在進入 code review 之前就自動解決——對 Monorepo 這種跨 package 改動頻繁的專案，人工 code review 的初篩成本接近歸零。

<AnnotationCard
  relevance={4}
  skills={["ai-workflow", "ci-cd", "monorepo-pnpm"]}
  adjacent="Cursor Cloud Agents + Git Worktree"
  adjacentNote={"你已有 Monorepo + CI/CD 基礎——下一步：在 Nexus UI 的跨 package 重構任務上試用 Cursor Cloud Agents，把不同 package 的修改派給不同 agent 並行跑，觀察 worktree 隔離是否真的避免了 context 污染。"}
  connection={"BugBot Autofix 35% merge rate 直接對應你在 EQS/MES 專案裡人工 code review 耗時的問題——你懂 Angular CDK 的元件邊界，這個背景讓你可以更精準地設定 BugBot 的 scope，讓它只審查你真正在意的 pattern。"}
/>

---

## Long-Horizon Agents：supervised 工作流是真實可用的，全自動是假設

如果你的 agent 有 CLI 存取、可讀 log、能跑測試、任務相互獨立——你現在已經可以讓它執行有意義的長任務，不需要等什麼「真正的 AGI」。BugBot Autofix 的數字說得很清楚：35% 的 AI 提出的 PR 修復被直接合併，但剩下的 65% 裡有相當比例是 agent 通過了測試卻沒有達成原始意圖——narrow verifier 過了、使用者感受沒到位。這是當前 long-horizon agent 的邊界。

軟體之所以是 AI agent 最好的實驗場，是因為軟體可測、可逆、可驗證：agent 可以直接執行測試確認輸出是否正確，不需要人在中間做模糊判斷——這個特性在其他領域幾乎不存在。有效的 long-horizon agent 模式是：給 CLI 存取、給文件存取、加 preflight check、跑 headless test，讓 agent 在 feedback loop 裡自我修正。複雜的多 agent 階層協調（manager → 多層 worker）目前仍然脆弱，simpler planner/worker 結構在多次測試中勝過複雜方案。

以前你的直覺是「讓 AI 跑，出錯再介入」；現在正確的期待是：你是 demanding QA，不是旁觀者。agent 在跑、你隨時可以介入，但不需要逐步下指令——這個 supervised autonomy 姿態，才是 2026 年 AI agent 工作流的實際型態。全自動的誘惑很大，但 35% merge rate 告訴你：另外 65% 需要你的判斷。

<AnnotationCard
  relevance={5}
  skills={["ai-workflow", "ci-cd"]}
  adjacent="Supervised Autonomy Pattern"
  adjacentNote={"你正在建 Claude_auto 排程 agent——下一步：為每個 session step 加入輕量 preflight check（讀取 idempotency flag）+ verification（重新讀剛寫的檔案確認 MDX 格式），把 agent drift 在早期攔截，不要等到 Vercel build 失敗才發現。"}
  connection={"這篇描述的 supervised autonomy 就是你現在 Claude_auto 工作流的實際運作方式——你的 session-log 就是 audit trail，你每次 session 的幂等性檢查就是 preflight check，你在 Step 4 的手動 Read 驗證就是 headless test 的人工版本。"}
/>

---

## 今日總結

今天的主題高度聚焦在 **AI agent 工具的成熟化**，從工具層到架構層到思維層：

1. **Claude Code v2.1.88** 是一次把體驗做紮實的修復版本。Compound command hook 精準觸發修復對你的 Claude_auto 排程工作流直接有用——如果你有用 hooks 做自動化，值得立刻更新。

2. **Cursor 2.5** 展示了 2026 年 AI coding tool 的下一個形態：不是更聰明的 AI，是更好的 agent 協調架構。8 個隔離 VM 並行跑、BugBot 從審查員升級為自動修復者（35% merge rate）——這個數字也定義了邊界。

3. **Supervised Autonomy** 是今天最重要的觀念更新。Long-horizon agent 的實際價值不在於「全自動」，而在於讓你用 demanding QA 的姿態監督——agent 跑，你在邊界介入。65% 的 BugBot Autofix 沒被 merge，說明「通過測試」≠「達成意圖」，這個 gap 需要人填。

**應用點：** 在 Claude_auto 每個 step 加入輕量 verification（重新讀剛寫的檔案），避免 agent drift 累積到後期才被 Vercel build 錯誤發現。
